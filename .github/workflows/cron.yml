name: cron
on:
  # push:
  #   branches:
  #     - '*'
  schedule:
    - cron: '35 * * * *'

jobs:
  build:
    name: cron-test
    runs-on: ubuntu-latest

    steps:
    - name: Hello
      run: |
        echo hello
        date
        set

